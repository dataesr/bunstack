{
  "name": "server",
  "version": "0.0.0",
  "dependencies": {
    "@elysiajs/static": "^1.1.1",
    "@elysiajs/swagger": "^1.1.3",
    "elysia": "^1.1.17"
  },
  "scripts": {
    "dev": "bun run --watch index.ts",
    "build": "bun build --compile --minify --target bun --outfile ../build/server index.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.1.10"
  }
}
